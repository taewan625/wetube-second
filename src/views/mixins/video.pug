//- mixin: Data를 받을 수 있는 일종의 미리 만들어진 HTML block.
mixin videoMixin(info)
    div
        h4=info.title
        ul
            li #{info.rating}/5
            li #{info.views} views.
            li id: #{info.id}